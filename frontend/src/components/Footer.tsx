export default function Footer() {
    return (
        <footer className="footer">
            <div className="container">
                © {new Date().getFullYear()} Quickshell Components Store
            </div>
        </footer>
    );
}
